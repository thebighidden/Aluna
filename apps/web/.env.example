NEXT_PUBLIC_APP_URL=http://localhost:3000
# Browser requests use the same-origin /api proxy by default.
# Override the private API origin used by the Next.js rewrite when needed.
ALUNA_API_ORIGIN=http://127.0.0.1:3001
